@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Punnel API</h1>
    <p><a href="#">Learn more &raquo;</a></p>
    <div class="container">
        <input type="text" id="message" />
        <input type="button" id="sendmessage" value="Send" />
        <input type="hidden" id="displayname" />
        <ul id="discussion"></ul>
    </div>
</div>

@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.3.0.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
        $(function () {
            // Reference the auto-generated proxy for the hub.
            var chat = $.connection.notificationHub;
            //$.connection.hub.url = 'https://api.punnel.com/signalr/hubs';
            $.connection.hub.qs = "token=bearersANziPnM7VidlkBHtHziEccxP2rLNt7pnldOB7z5h2K8rQFWHMO0ut6s9U41qLs_ADdiRqjYAiApiHxh_VUtEHD9lPMQPquWOQQ04pyPBw-Z3o6RV23uLmAGRhrqHmshZoUjn5x2Zp3guvP2xGPFuig013vjVUwx2zcAWbBp1io7o-eBi6H7cHICKvLBBadWQ7F9JKCimdkrItGQPYm2EHtipZj9Mkm3qDF-8nFVUN_PPJcwGjdU7MSE75_Kmi3dib7N387FqBWktA91J8YHVkdN0kKbZgEgrVzzuPMHxwLImDEnPk9BYE77hREnhq39Wdju-5mlT4KKFTZ1JY_owERLO0XMdIey6q1zL0IMf0czvWh66ZaCVwVkQq-2veDgdRbyZw-wdmCImmY9xa_CqP0MzcP7V1-eQYKQA1j6kVw-httAPw08a_hE4SNI0l3b";
            chat.client.notifyMsg = function (message) {
                $('#discussion').append('<li>' + htmlEncode(message) + '</li>');
            };
            // Create a function that the hub can call back to display messages.
            chat.client.unview_receive = function (total) {
                // Add the message to the page.
                $('#discussion').append('<li><strong>' + total + '</li>');
            };
            // Get the user name and store it to prepend to messages.
            //$('#displayname').val(prompt('Enter your name:', ''));
            // Set initial focus to message input box.
            //$('#message').focus();
            // Start the connection.
            $.connection.hub.start({ jsonp: true }).done(function () {
                console.log('connected');
                $('#sendmessage').click(function () {
                    // Call the Send method on the hub.
                    chat.server.unView('7a5d2d39-0d39-452c-9e6b-d7bc6010341e');
                    // Clear text box and reset focus for next comment.
                    $('#message').val('').focus();
                });
            }).fail(function (res) {
                console.log(res);
            });

            //$.connection.hub.start().done(function () {
            //    $('#sendmessage').click(function () {
            //        // Call the Send method on the hub.
            //        chat.server.send($('#displayname').val(), $('#message').val());
            //        // Clear text box and reset focus for next comment.
            //        $('#message').val('').focus();
            //    });
            //});
        });
        // This optional function html-encodes messages for display in the page.
        function htmlEncode(value) {
            var encodedValue = $('<div />').text(value).html();
            return encodedValue;
        }
    </script>
}
